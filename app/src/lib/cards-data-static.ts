// Static cards data for client-side use
// This file is generated at build time from the CSV

import { CreditCard } from './types';

// Placeholder - will be replaced with actual data at build time
export const staticCards: CreditCard[] = [];

// Function to get cards - will be populated at build time
export function getStaticCards(): CreditCard[] {
  return staticCards;
}
